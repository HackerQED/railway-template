# 上线前最后冲刺清单

> 日期：2026-03-22
> 状态：功能开发已封闭，进入收尾阶段

## 当前定位

- **产品功能**：All-in-One AI 视频图片生成平台（图片生成 + 视频生成）
- **SEO 策略**：启动阶段靠**模型词子页面**获客（seedream, veo 等），不依赖 "AI Music Video Generator" 大词
- **MV 大词**：SEO 先占坑，等赚到钱再投资做 MV 功能来匹配
- **Agent 侧**：Skill 安装入口保留，作为差异化亮点但不绑死任何平台
- **OpenClaw**：布局方向 + SEO 关键词，不作为一期主打

## 一期已完成的能力

- ✅ Seedream 图片生成（KIE provider，真实实现）
- ✅ Veo 视频生成（KIE Veo provider，真实实现）
- ✅ Worker 轮询 + 异步任务系统
- ✅ Agent 三层发现机制（llms.txt → Skill → API 文档）
- ✅ CAPABILITIES 单一数据源
- ✅ 认证系统（Google OAuth + dev email/password）
- ✅ 积分基础架构
- ✅ Project + Preview 系统
- ✅ 双轨鉴权（API Key + Session Cookie）
- ✅ 文件上传（upload）
- ✅ 异步任务状态查询（generations/status）

## 需要清理的部分

### 移除 / 隐藏未实现的能力

音乐分析（music/analyze）和视频合成（video/compose）目前是 Mock，一期不对外暴露：

- [ ] `src/lib/api-capabilities.ts` — 从 CAPABILITIES_MAP 中移除或标记为 hidden，不在 capabilities JSON 和 llms.txt 中暴露
- [ ] `skills/yino-ai/SKILL.md` — 移除对 music/analyze 和 video/compose 的引用
- [ ] `content/docs/tools/music-analyze.mdx` + `video-compose.mdx` — 保留文件但不链接（后续重新上线时直接启用）
- upload、projects、preview、generations/status 已确认是真实实现，保留暴露

## 上线冲刺清单

### P0：上线必须

#### 1. Hero 区域重做（可灵风格）
- 核心：大输入框 + "Generate" 按钮，视觉上像 SuperMaker/可灵
- 输入框上方放引导安装 Skill 的提示条（差异化亮点）："Install as a skill in Claude Code / Codex / OpenClaw"
- 点击提交 → 跳转到具体生成页面，prompt 自动填入（先做假的，不做首页直接生成）
- 输入框下方放 Hints 标签（如 "Cinematic Scene", "Anime Style" 等快捷提示）
- 去掉当前的 chat 模拟界面

#### 2. HomePage.json 文案重写
- 产品定位从 "OpenClaw Music Video Generator" → "All-in-One AI Video & Image Generator"
- SEO 标题/描述保留 "AI Music Video Generator" 关键词
- Features 聚焦已有能力：图片生成、视频生成、批量处理、Agent API
- FAQ 去掉 Music Video 端到端流程的具体承诺，改为更通用的表述
- MV 相关内容保留但定位为"可通过 Agent 工作流实现的进阶能力"，已手动验证过，尚未完全集成
- Testimonials 检查是否有与未实现功能相关的描述

#### 3. Credit 系统 + 定价方案
- 确定各模型的 credit 消耗量（需要先完成供应商比价）
- 定价页面填入真实数字
- 注册赠送额度 + 月度免费额度确定

#### 4. 供应商比价与成本核算
- [ ] 确认 KIE 是否支持 WaveSpeed 的全部参数（应该是有，需验证）
- [ ] Seedream：KIE vs 其他供应商的价格对比
- [ ] Veo：KIE vs 其他供应商的价格对比
- [ ] 确认 Seedream 是否有 Auto 模式

#### 5. 部署准备
- [ ] Google OAuth 回调 URL 配置到生产域名
- [ ] Stripe webhook endpoint 更新为生产 URL
- [ ] Railway 环境变量配置（DATABASE_URL、BETTER_AUTH_SECRET、API Keys 等）
- [ ] Railway 部署 dry run — 至少完整跑一次构建 + 启动
- [ ] `NEXT_PUBLIC_BASE_URL` 设为实际域名
- [ ] 域名 DNS 配置

### P1：上线后尽快

#### 6. 模型选择器 UI 简化
- 当前设计太复杂（Context + 多层组件）
- 改为弹窗 → 链接跳转到各模型页面
- 降低耦合，各模型页面独立

#### 7. 全局模型/功能注册表重构
- 将 API Capability 注册表（`api-capabilities.ts`）和前端模型配置（`config/models.ts`）统一
- 单一注册表驱动：API 发现、前端 UI、定价、文档

#### 8. 落地页素材 + SEO 页面
- 模型页面（/models/seedream, /models/veo-3 等）素材准备
- SEO 着陆页文案
- OG 图片 / 社交分享素材

#### 9. 模型体验优化
- Seedream Auto 模式确认与 UI 适配
- 生成结果的展示优化
- 错误状态的用户提示

### P2：后续迭代

- Music Video 端到端工作流（music/analyze + video/compose 真实实现）
- OpenClaw 共享 Demo（方案已设计，暂时搁置）
- Skill 知识层优化（knowledge/ 文档质量提升）
- 批量处理 UI

## 风险点

| 风险 | 影响 | 缓解 |
|------|------|------|
| KIE API 稳定性未经长时间验证 | 生产环境生成失败 | 上线前压测 + 错误重试机制检查 |
| Credit 定价定错 | 亏钱或用户觉得贵 | 先定保守价格，观察后调整 |
| SEO 主打 Music Video 但产品没有 MV 功能 | 用户预期不匹配 | 启动阶段不依赖 MV 大词获客，靠模型词子页面引流；MV 文案作为愿景保留，等赚到钱再投资做功能 |
